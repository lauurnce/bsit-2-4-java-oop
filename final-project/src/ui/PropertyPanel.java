package ui;

import javax.swing.*;

/** STUB — full implementation in a later task. */
public class PropertyPanel extends JPanel {
    public PropertyPanel() { add(new JLabel("Properties — coming in Task 10")); }
}
