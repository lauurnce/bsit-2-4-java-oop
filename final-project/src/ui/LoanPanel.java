package ui;

import javax.swing.*;

/** STUB — full implementation in a later task. */
public class LoanPanel extends JPanel {
    public LoanPanel() { add(new JLabel("Loans — coming in Task 11")); }
}
